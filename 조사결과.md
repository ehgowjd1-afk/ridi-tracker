# 리디(RIDI) 사이트 조사 결과 — 2026-08-22

로그인 없이 브라우저로 직접 확인한 내용. 수집기 만들 때 이 문서를 기준으로 삼는다.

---

## 1. 결론 요약

| 항목 | 결과 |
|---|---|
| 랭킹 최대 순위 | **정확히 200위까지** (숨은 순위 없음. API가 limit>200을 거부) |
| 랭킹 기간 | DAILY / WEEKLY / MONTHLY / YEARLY / STEADY — 5종 모두 200위씩 |
| 작품 상세정보 | 랭킹 API 안에 대부분 포함 (별도 요청 불필요) |
| 태그·키워드 | 상세페이지에 있음 (+ 로맨스는 랭킹 API의 romance_guide에도 있음) |
| 리뷰 | GraphQL로 로그인 없이 수집 가능 |
| 독점 표시 | is_original / is_only / is_pre_exclusive 필드로 제공 |
| 19금 작품 | **로그인 없이 랭킹·상세 데이터 모두 나옴** |
| 19금 이벤트 | **목록은 나옴 / 이벤트 상세페이지는 로그인 필요** |

---

## 2. 랭킹 API (핵심)

```
GET https://api.ridibooks.com/v2/bestsellers
  ?category_includes={카테고리ID}
  &limit=200            # 최대 200. 201 이상은 400 에러
  &period=DAILY         # DAILY|WEEKLY|MONTHLY|YEARLY|STEADY
  &only_rent_available=0
```

- offset=200 이상은 빈 배열 → **리디가 공개하는 랭킹은 200위가 전부**
- 부모 카테고리 ID(예: 1650)도 그대로 동작
- 응답: `data.items[]` (배열 순서 = 순위), `data.recommended_bestseller`, `data.pagination`

### 수집 대상 카테고리

**웹소설 — 웹소설(연재)**
| 장르 | ID |
|---|---|
| 로맨스 웹소설 | 1650 (하위 1651 현대물, 1652 역사/시대물) |
| 로판 웹소설 | 6050 (6051 동양풍, 6052 서양풍, 6053 가상세계) |
| 판타지 웹소설 | 1750 (1751 정통, 1752 퓨전, 1753 현대, 1754 무협) |
| BL 웹소설 | 4150 (4151 현대, 4152 판타지, 4153 역사/시대) |

**웹소설 — E북 단행본**
| 장르 | ID |
|---|---|
| 로맨스 e북 | 1700 (1701 현대, 1702 역사, 1704 할리퀸, 1705 하이틴, 1706 19+, 1708 TL, 1709 섹슈얼) |
| 로판 e북 | 6000 (6001 동양풍, 6002 서양풍, 6003 해외, 6004 가상세계) |
| 판타지 e북 | 1710 (1711~1716, 1721 신무협, 1722 전통무협) |
| BL 소설 e북 | 4100 (4101~4104) |
| 라이트노벨 | 3000 (3001 해외, 3002 TL, 3005 국내, 3006 성인) |

**웹툰**
| 장르 | ID |
|---|---|
| 웹툰 전체 | 1600 |
| 로판 1612 / 로맨스 1613 / 드라마 1603 / 성인 1604 / 액션·무협 1605 / 판타지·SF 1606 / 스포츠·학원 1607 / 코믹 1608 / GL 1609 / 공포·추리 1610 / 19+ 1614 | |
| BL 웹툰 | 4250 (4251, 4252) |

### 랭킹 API가 주는 필드 (작품 1개당)

- `book_id`, `title`, `web_title`, `cover.{small,large,xxlarge}` — 표지 이미지
- `authors[]` — 이름 + role(author/translator/illustrator 등)
- `publisher.{publisher_id,name}` — 출판사/CP
- `categories[]` — 장르 분류
- `ratings[]` — **별점 분포(1~5별 각각 몇 명)** → 평균 별점 + 참여자 수 계산 가능
- `introduction.description` — 작품 소개
- `introduction.romance_guide` — **작품 키워드 + 남주/여주 소개** (로맨스 계열)
- `introduction.author_bio`, `table_of_contents`, `publisher_review`
- `adults_only` — 19금 여부
- `is_original` / `is_only` / `is_pre_exclusive` — **독점 여부 3종**
- `serial.total` — 총 회차 수, `serial.completion` — 완결 여부
- `serial.unit` — "화" / "권"
- `serial.wait_for_free` — **기다무 정보** (대기시간, 시작일, 종료일)
- `serial.free.{purchase,rental}` — 무료 회차 수
- `purchase.{full_price,sale_price}`, `serial.purchase_all.{full_price,sale_price,discount_rate}` — 가격·할인율
- `badges[]` — 화면 배지 (Up, N% 할인 등)
- `registration_date`, `publication_date`, `file.character_count`

→ **랭킹 API 하나로 사용자가 요청한 상세 항목이 거의 다 채워진다.**

---

## 3. 이벤트 API

```
GET https://api.ridibooks.com/v2/events
  ?genres[0]={장르}&status=ongoing&platform=web&limit=100&offset=0
```

허용 장르값: `bl, bl_comic, bl_novel, bl_serial_comic, bl_serial_novel, bl_webnovel,
bl_webtoon, comic, comic_serial, fantasy, fantasy_serial, general, lightnovel,
romance, romance_fantasy, romance_fantasy_serial, romance_serial, webtoon`

- 현재 진행 중 이벤트 **369건** (중복 제거) 확인
- 필드: `id, title, description, startDate, endDate, bannerImageUrl, link, type`
- 19금 이벤트도 목록에는 정상 노출 (예: 🔞 표시 이벤트, "고수위 로맨스 모음.Zip")

### 이벤트 ↔ 작품 연결
- 이벤트 상세페이지(`/event/{id}`)의 작품 목록은 iframe 지연 로딩 → 수집 어려움
- **19금 이벤트 상세페이지는 로그인으로 리다이렉트됨** (예: /event/25832 → /account/login)
- ✅ **대안(권장): 작품 상세페이지에서 거꾸로 수집.**
  작품 페이지에 그 작품에 걸린 이벤트 링크가 그대로 노출됨
  (예: 악인 남편 → /event/96980, /event/62263, /event/61442)
  로그인 불필요. 이 방향이 훨씬 안정적.

---

## 4. 작품 상세페이지

`https://ridibooks.com/books/{book_id}` — 레거시 SSR 페이지

- `<script id="ISLANDS__PreparedData">` 안에
  `props.gridQuery.riGrid.grid.meta.gridPageMeta.keywords[]` → **태그·키워드 배열**
  (예: 환생/회귀, 왕족/귀족, 계략남, 기다리면무료, 원작소설有, 연재, CP사명, 작가명)
- 같은 JSON의 `grid.cells[]`에 `BookDetailHomeReview` 셀의 **UUID** 존재 → 리뷰 수집에 필요
- `<title>`에 "리디에만 있는 독점 작품!" 문구 → 독점 여부 보조 확인
- 페이지 내 `/event/{id}` 링크 → 그 작품에 걸린 이벤트

---

## 5. 리뷰 수집 (로그인 불필요)

```
POST https://api.ridibooks.com/graphql
```

query: `BookDetailHomeReviewsPagination($id: UUID!, $context: BookDetailHomeReviewCellContext!)`

variables 예시:
```json
{
  "id": "<상세페이지 PreparedData의 BookDetailHomeReview 셀 UUID>",
  "context": {
    "bookId": "6039000001",
    "buyerOnly": true,
    "order": "RECENT",
    "pageLimitInput": { "limit": 10, "page": 1 }
  }
}
```

응답 필드: `rating`(1~5), `content`, `timestamp`, `likeVoteCnt`, `isBuyer`,
`userId`(마스킹: "yun***"), `comments[]`, `pagination.hasMore`

- order 옵션: RECENT / (공감순·별점순도 UI에 존재)
- buyerOnly: true=구매자만, false=전체
- 예시 작품 기준 구매자 2,521건 / 전체 3,121건 → 전량 수집은 요청 과다.
  **작품당 최신 30~50건만** 받는 것을 권장.

---

## 6. 기타 유용한 API

```
독점작:   /v2/pre-exclusives?adult_option=none&genre={장르}&limit=50&offset=0
신작:     /v2/new-releases?category_includes={ID}&limit=50
실시간:   /v2/reading-books?category_includes={ID}&limit=50
기획전:   /v2/selections?limit=50&platform=web&section_id={ID}
페이지구성:/v2/... (genreHomeView 로 섹션 목록 확인 가능)
```

---

## 7. 수집 부하 계산

- 랭킹: 카테고리 약 30개 × 기간 2~3종 = 60~90 요청/일
- 상세(태그·이벤트): 신규 등장 작품만, 하루 100건 상한
- 리뷰: 상위권 작품 위주, 하루 100건 상한
- 요청 간격 1.5~2초 → 총 소요 10~15분, 하루 1회

---

## 8. 개발 환경 현황

- Git 2.55.0 ✅ 설치됨
- Python ❌ 미설치 (Microsoft Store 스텁만 있음 → 실제 파이썬 설치 필요)
- Node.js ❌ 미설치 (이번 프로젝트엔 불필요)
